const a = 1
const b = 1
console.log(a-b)
//we need to ignore the file before commiting it 
// if it is hashed once we need to unhash it inorder to ignore 
//Two ways to create a branch 
// git checkout -b name.newfile
// git branch name.newfile